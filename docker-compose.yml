services:
  app:
    build:
      context: .
      args:
        NEXT_PUBLIC_VAPID_PUBLIC_KEY: ${NEXT_PUBLIC_VAPID_PUBLIC_KEY}
    restart: unless-stopped
    depends_on:
      - db
    environment:
      DATABASE_URL: ${DATABASE_URL_DOCKER:-postgres://bossuyt:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-bossuyt}}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
      NEXTAUTH_URL: ${NEXTAUTH_URL}
      NEXT_PUBLIC_VAPID_PUBLIC_KEY: ${NEXT_PUBLIC_VAPID_PUBLIC_KEY}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      - "traefik.http.routers.bossuyt-staging.rule=Host(`staging.bossuyt.fixassistant.com`)"
      - "traefik.http.routers.bossuyt-staging.entrypoints=websecure"
      - "traefik.http.routers.bossuyt-staging.tls.certresolver=lets-encrypt"
      - "traefik.http.services.bossuyt-staging.loadbalancer.server.port=3000"
    networks:
      - traefik
      - internal

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-bossuyt}
      POSTGRES_USER: ${POSTGRES_USER:-bossuyt}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal

volumes:
  postgres_data:

networks:
  traefik:
    external: true
  internal:
    driver: bridge
